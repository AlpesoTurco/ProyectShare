<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">

    <title>EduShare</title>
</head>
<body>
    <div class="min-h-screen flex items-center justify-center bg-gray-100" style="background-image: url('resources/img/Fondo2.jpg');">
        <div class="bg-white p-8 rounded-lg shadow-md w-96 opacity-95">
            <h2 class="text-2xl font-semibold mb-4">Inicia Sesión</h2>
            <form action="/auth" method="post">
                <input type="text" name="user" id="user" placeholder="Correo..." required class="mt-1 p-2 w-full border rounded-md" value="alvaroorozco124578@gmail.com">
                <input type="password" name="pass" id="pass" placeholder="Contraseña..." required class="mt-4 p-2 w-full border rounded-md" value="a">
                <input type="submit" class="mt-4 w-full bg-blue-500 text-white p-2 rounded-md hover:bg-blue-600 focus:outline-none focus:border-blue-700 focus:ring focus:ring-blue-200" value="Login">
            </form>
            <a href="register" class="block text-center mt-4 text-blue-500 hover:text-blue-600">Registarse</a>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
    <%if (typeof alert != "undefined") { %>
        <script>
            Swal.fire({
                title: '<%= alertTitle %>',
                text: '<%= alertMessage %>',
                icon: '<%= alertIcon %>',
                showConfirmButton: '<% showConfirmButton %>', 
                timer: '<%= timer %>' 
            }).then(() => {
                window.location = '/<%= ruta %>';
            });
        </script>
    <% } %>
</body>
</html>
